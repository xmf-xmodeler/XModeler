parserImport XOCL;

import MDC;
import Displays;

context Displays

  @Class SideBars extends Box
  
    @Operation init(values)
      self.showTop := false;
      self.showBottom := false;
      self.fill := false;
      super(values)
    end
    
  end