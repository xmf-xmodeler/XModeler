parserImport XOCL;

import XSync;
import Network;

context Network

  @Class ScopedType 
  
    @Attribute type        : Classifier  (?) end
    @Attribute modifiers   : Seq(String)     end
    @Attribute newListener : Operation       end
    
    @Constructor(type,modifiers,newListener) ! end
  
  end