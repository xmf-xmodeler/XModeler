parserImport XOCL;

import XSync;
import Network;

context Network

  @Class DirectedLocation extends Location
  
    @Attribute location : Location (?) end
    
    @Constructor(location) ! end
  
  end