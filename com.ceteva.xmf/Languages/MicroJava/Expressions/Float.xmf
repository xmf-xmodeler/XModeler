parserImport XOCL;

import Languages;
import MicroJava;
import Expressions;

context Expressions

  @Class Float extends JExp
  
    @Attribute value : Float (?) end
    
    @Constructor(value) ! end
    
    @Operation pprint(out)
      format(out,"~S",Seq{value})
    end
    
  end