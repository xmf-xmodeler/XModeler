package com.ceteva.text.actions;

import org.eclipse.ui.part.EditorActionBarContributor;

public class BrowserWorkbenchContributions extends EditorActionBarContributor {
	
	/* private void doSetActiveEditor(IEditorPart part) {
		HTMLViewer viewer = (part instanceof HTMLViewer) ? (HTMLViewer) part : null;
		IActionBars actionBars= getActionBars();
		if (actionBars != null)
		  actionBars.setGlobalActionHandler(ITextEditorActionConstants.PRINT,viewer.);
	} */
}